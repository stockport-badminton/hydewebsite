<div class="page-header">
  <div class="container">
    <h1><%= pageHeading %></h1>
    <% if (typeof pageSubheading !== 'undefined' && pageSubheading) { %>
    <p class="page-header-subheading"><%= pageSubheading %></p>
    <% } %>
  </div>
</div>
